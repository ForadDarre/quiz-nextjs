DB:
-   Add new entity - Quiz
-   Add QuizId to Questions
-   Populate Quizzes
-   Populate Questions

BE:
-   Add QuizesController
-   Add API endpoint to get all quizzes
-   Add API endpoint to get all questions for quiz with {Id}

FE:
-   Add Main page (you can select quizes there)
-   Quizzes will be populated on build (use generateStaticParams)
-   When user selects a quiz, we redirect a user to /quiz/{id}
-   On /quiz/{id} (I guess it is a page.tsx file inside the [id] folder) do fetchQuestions and display all the questions
-   Add correct funcionality to the /quiz/{id} page

Containerazation
-   Add all the docker things